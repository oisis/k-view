# Node resources cannot be created via YAML manifests as they are registered by kubelet.
# This file serves as a placeholder for the 27th resource in the test suite.
# Your Docker Desktop cluster already has at least one node: docker-desktop.
