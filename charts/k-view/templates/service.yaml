apiVersion: v1
kind: Service
metadata:
  name: {{ include "k-view.fullname" . }}
  labels:
    {{- include "k-view.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "k-view.selectorLabels" . | nindent 4 }}
